---
phase: 01-project-foundation
plan: 01
type: execute
---

<objective>
Set up Maven project structure with pom.xml, manifest.json, and directory layout for HytaleVotifier plugin.

Purpose: Establish the foundational build system and plugin metadata that all subsequent phases depend on.
Output: Compilable Maven project with proper Hytale plugin structure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Discovery findings (from nitrado/hytale-plugin-query reference):**

Maven pom.xml pattern:
- groupId: net.nitrado.hytale.plugins (use org.hyvote.plugins for our project)
- Java 25 via maven.compiler.source/target
- HytaleServer-parent dependency (com.hypixel.hytale, 1.0-SNAPSHOT, provided)
- nitrado-webserver dependency (net.nitrado.hytale.plugins, 1.0.0, provided)

manifest.json structure:
- Group: "Hyvote" (matching endpoint prefix)
- Name: "HytaleVotifier"
- Version, Authors, Website, Main class path
- Dependencies: {"Nitrado:WebServer": "*"}

Directory structure:
- src/main/java/org/hyvote/plugins/votifier/
- src/main/resources/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pom.xml with dependencies</name>
  <files>pom.xml</files>
  <action>
Create Maven pom.xml at project root with:
- groupId: org.hyvote.plugins
- artifactId: hytale-votifier
- version: ${revision} with revision property set to 1.0.0-SNAPSHOT
- Java 25 compiler source/target
- UTF-8 encoding
- Dependencies (both provided scope):
  1. com.hypixel.hytale:HytaleServer-parent:1.0-SNAPSHOT
  2. net.nitrado.hytale.plugins:nitrado-webserver:1.0.0

Do NOT add build plugins or repositories - the reference project doesn't have them and they're not needed for basic compilation.
  </action>
  <verify>mvn validate passes (validates pom.xml structure)</verify>
  <done>pom.xml exists, mvn validate succeeds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create manifest.json with plugin metadata</name>
  <files>src/main/resources/manifest.json</files>
  <action>
Create manifest.json in src/main/resources/ with:
- Group: "Hyvote"
- Name: "HytaleVotifier"
- Version: "1.0.0"
- Authors: [{"Name": "HytaleVotifier Contributors", "Url": "https://github.com/hyvote/hytale-votifier"}]
- Website: "https://github.com/hyvote/hytale-votifier"
- Main: "org.hyvote.plugins.votifier.HytaleVotifierPlugin"
- ServerVersion: "*"
- Dependencies: {"Nitrado:WebServer": "*"}

This registers endpoints at /Hyvote/HytaleVotifier/ path prefix per WebServer plugin conventions.
  </action>
  <verify>cat src/main/resources/manifest.json | python3 -m json.tool (validates JSON syntax)</verify>
  <done>manifest.json exists, is valid JSON, contains all required fields</done>
</task>

<task type="auto">
  <name>Task 3: Create source directory structure</name>
  <files>src/main/java/org/hyvote/plugins/votifier/.gitkeep</files>
  <action>
Create the Java source directory tree:
- src/main/java/org/hyvote/plugins/votifier/

Add a .gitkeep file to ensure the directory is tracked by git (will be removed when we add actual source files in the next plan).

Do NOT create any Java classes yet - that's Plan 01-02's responsibility.
  </action>
  <verify>ls -la src/main/java/org/hyvote/plugins/votifier/ shows directory exists</verify>
  <done>Directory structure exists with .gitkeep placeholder</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pom.xml exists at project root
- [ ] mvn validate passes
- [ ] src/main/resources/manifest.json exists and is valid JSON
- [ ] src/main/java/org/hyvote/plugins/votifier/ directory exists
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Project has valid Maven structure
- Plugin metadata correctly configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
